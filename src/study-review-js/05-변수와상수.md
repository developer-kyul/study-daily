✨2026-01-02 금요일

# **변수와 상수**

변수를 사용해 코드의 흐름, 프로그래밍의 맥락을 구성할 수 있다.

4 + 27 → 4는 뭐임, 27은 뭐임, 왜 더하는 거임

// 의미를 담은 변수 이름

가지고*있는*사과\_갯수 = 4

추가*구매한*사과\_갯수 = 27

//계산 과정

총*사과*갯수 = 가지고*\*있는**사과\_*갯수 + 추가*\*구매한**사과\_*갯수

## **변수 선언**

**`let`** 키워드를 사용해 변수를 선언

변수 선언에는 영어로 선언하는 것을 권장(변수명은 알아보기 쉽게 줄여쓰지 말기)

변수 선언시 작성해야하는 부분

![alt text](./src/img/image2.png)

변수 이름은 연결된 한 단어로 작성되어야 한다.

```jsx
//한 단어로 연결이 안될 때 발생하는 상황, 연결이 안된 뒷 부분은 컴퓨터가 읽지 못함
let 가지고; // 가지고 변수 선언됨
있는*사과*개수 = 4 // 있는*사과*개수 변수는???? 모르겠다. 오류!!!

//프로그램에서 허용하지 않는(정의되지 않은) 문자를 변수 이름으로는 사용 가능

let truthy = true
let falsy = false
let yes = true
let no = false
let 할게요 = true
let 안할게요 = false

//선언된 변수 값 재할당

let truthy = true
let truthy = false // 해당 코드 오류로 나옴
truthy = false // 같은 이름을 다시 사용하고 싶으면 let 없이 재할당해야함

//선언만 하고 나중에 할당
let gamePlayer // 변수 선언
gamePlayer = '라모스' // 선언된 변수에 값을 할당
console.log(gamePlayer)

// 여러 변수 동시 선언
// let screen_width = 290
// let screen_height = 842
// 여러 번수 선언과 동시에 값 할당
let x = 4, y = 8

// 여러 변수 선언만 (값 없이)
let screen_width, screen_height
// 이후에 선언된 변수에 값 할당
screen_width = 290
screen_height = 842
console.log('screen_width :>> ', screen_width)
console.log('screen_height :>> ', screen_height)
```

---

띄어쓰기 가 안되는 이유

let 가지고 있는*사과*개수 =4; 선언 시

띄어쓰기 사용 시 컴퓨터는 세미콜론을 붙이기 때문에 가지고 라는 변수는 선언 되지만

다음에 오는 있는*사과*개수 는 let이나 const와 같은 키워드 가 없이 선언 되기 때문에 오류가 발생합니다

let 가지고; // 가지고 변수 선언됨

있는사과개수 = 4 // 있는사과개수 변수는???? 모르겠다. 오류!!!

### **변수 이름 작성 규칙**

변수는 의미 있는 코드를 작성하기 위해 사용되서, 변수 이름은 의미 있게 작성해야 함

- 올바른 변수 이름
- 연결된 한 단어로 작성
- 알파벳(a-z, A-Z), 숫자(0-9) 그리고 **`_`** , **`$`** 등의 기호로 구성
- 명확하고 구체적으로 작성하고(user, userAges), 의미를 알 수 있어야함(maxLoginAttempts, shoppingCartItems)
- 잘못된 변수 이름
- 공백 사용 불가
- \_, $를 제외한, 특수문자(-, #, @, !, ...) 사용 불가
- 숫자로 시작 불가
- 예약어(continue, break, let, const, new, class, ...) 사용 불가

### 변수 이름 표기법

```jsx
// 소문자 (lowercase) - 간단한 변수
let email;

// 대문자 (UPPERCASE) - 상수(constant) - 상수는 관례규칙상 전체 대문자로 작성
let ADMINISTRATOR;
let BONUS_POINT = 100;

// 스네이크 케이스 (snake_case) - 변수 이름 권장
let margin_block_start;
let user_email;
let user_password;

// 카멜 케이스 (camelCase) - 변수 또는 함수 이름 권장
let paddingInlineEnd;
let userName, getTotalPrice;

// 파스칼 케이스 (PascalCase) - 클래스, 생성자
let SuperMario, User, ShoppingCart;

// 케밥 케이스 (kebab-case) - JavaScript에서 사용 불가 ❌
// ❌ 에러 발생!
// let text-decoration
```

### **const 상수**

변하지 않는 수

```jsx
// 상수 선언과 동시에 값 할당 (필수!)

const 상수 = '고정된 값';
console.log(상수);
// 상수는 재할당 불가능
// ❌ 에러 발생! TypeError: Assignment to constant variable.
상수 = '값 변경 시도';
// 상수는 선언하고 값을 따로 할당하면 에러 발생
// ❌ 에러 발생! SyntaxError: Missing initializer

const 상수;
상수 = '고정된 값';
// 여러 상수 동시 선언
const MIN_VALUE = 10,
  MAX_VALUE = 100;
console.log(MIN_VALUE, MAX_VALUE);

var 변수;
// var 변수 선언
var incrementCount = 9;
console.log(incrementCount);

// var는 재선언 가능 (문제가 될 수 있음)

// ✅ 에러 없음 (하지만 위험!)
var incrementCount = 21;
console.log(incrementCount);

// let은 재선언 불가능
// ❌ 에러 발생! SyntaxError
```
